# C 组智能选课测试报告摘录

> 本文件从 `docs/test_report/test-report.md` 中抽取 C 组相关内容，保留原章节号与标题等级，便于手动合并到最新版总测试报告。

---

## 0. 文档信息

### 0.2 测试分工

| 子系统编号 | 子系统名称 | 测试负责人 | 主要测试范围 |
|---|---|---|---|
| C | 智能选课 |  | 培养方案、选课、退课、AI 辅助、并发 |

---

## 2. 测试范围【全组统一 + A-F 分组确认】

### 2.1 本次测试覆盖范围

| 子系统 | 覆盖模块 | 是否覆盖 | 说明 |
|---|---|---|---|
| C | 培养方案、课程搜索、选退课、选课管理、AI 辅助 | 是 | 已覆盖后端规则、前端页面、AI 辅助、保存建议、准入租约和 200 在线用户压测 |

### 2.2 本次不覆盖范围

C 组补充：
- 已完成本地 Docker 环境单实例、双后端实例 200 虚拟用户压测，以及 200 人并发提交选课压测；由于课程作业无公网部署条件，真实公网链路压测不纳入本轮测试范围。
- AI 推荐已验证流程、降级、保存记录和 1 个 DeepSeek live 人工优质样例；推荐质量仍不按生产级长期评测口径评价。
- 教务阶段维护和手动加课均已完成 API 端到端验收和人工验收。

---

## 5. 测试用例设计【A-F 分组分别写】

### 5.4 C 智能选课测试用例【C 组填写】

| 用例编号 | 所属子系统 | 对应需求 | 测试名称 | 测试方式 | 实际结果 | 是否通过 |
|---|---|---|---|---|---|---|
| TC-C-001 | C | 培养方案 | 确认培养方案与学分进展 | 后端单元 + 前端组件 | 已区分已修、在修、未修状态 | 通过 |
| TC-C-002 | C | 课程搜索 | 课程搜索、详情与 eligibility | 后端单元 + 前端组件 | 可显示课程状态和不可选原因 | 通过 |
| TC-C-003 | C | 选课 | 正常选课、容量与并发保护 | 后端单元 | 事务创建 Enrollment 并更新容量 | 通过 |
| TC-C-004 | C | 选课异常 | 容量满、重复、冲突、超学分、未确认培养方案 | 后端单元 | 均按业务错误拒绝 | 通过 |
| TC-C-005 | C | 先修课 | 按有效成绩校验先修课 | 后端单元 | 已通过成绩可满足先修，未通过被拒绝 | 通过 |
| TC-C-006 | C | 退课 | allow_drop 配置驱动退课 | 后端单元 + 前端组件 | 允许阶段可退，不允许阶段拒绝 | 通过 |
| TC-C-007 | C | 选课结果/课表 | 当前选课、历史学期课表 | 后端单元 + 前端组件 | 可查询结果和历史学期课表 | 通过 |
| TC-C-008 | C | FR-C-15 | 选课准入租约 | 后端单元 | enter、heartbeat、leave、TTL 释放均通过 | 通过 |
| TC-C-009 | C | AI 辅助 | 推荐、解释、降级、保存建议 | 后端单元 + 前端组件 + live 人工样例 | 不写 Enrollment，保存记录可回看；DeepSeek live 推荐样例通过系统规则校验 | 通过 |

---

## 6. 测试执行结果【A-F 分组填写，统一汇总】

### 6.1 总体执行统计【全组统一整合】

| 子系统 | 用例总数 | 已执行 | 通过 | 失败 | 阻塞 | 未执行 | 通过率 |
|---|---:|---:|---:|---:|---:|---:|---:|
| C | 137 | 137 | 137 | 0 | 0 | 0 | 100% |

### 6.4 C 智能选课测试结果【C 组填写】

本次 C 组可直接执行的自动化、压测与人工验收项目均已通过；DeepSeek live LLM 推荐样例已完成人工验收，自动化 live LLM 用例仍按默认配置跳过。

| 类型 | 命令摘要 | 结果 |
|---|---|---|
| 后端回归测试 | `@stss/server test:unit -- ...course-selection...` | 31 个测试文件，605 个用例通过，live LLM 1 个跳过 |
| 前端 C 组测试 | `@stss/web test:run src/modules/course-selection` | 7 个测试文件、37 个用例通过 |
| AI live 推荐人工样例 | `cstudent01` + DeepSeek V4 Flash + 详细偏好说明 | 偏好解析和推荐生成均 HTTP 200；无规则降级；推荐课程均通过容量、冲突和先修规则校验 |
| 200 在线用户压测 | `C_LOAD_SCENARIO=online` | 单实例 200/200 通过，双后端实例 200/200 通过 |
| 200 并发提交压测 | `submit-capacity` / `submit-full` | 容量 50 无超卖；容量 200 重试后 200/200 成功 |
| 教务阶段管理端到端验收 | `academic` 更新阶段 + `cstudent01` 选退课验证 | 阶段列表、权限错误提示、全屏弹窗编辑、`allow_drop`、`max_credits`、停用阶段均通过 |
| 教务手动加课端到端验收 | `academic` 按学生和课程关键词选择后提交 | 可按学号/姓名和课程名检索选择；提交原因后学生选课记录、课表和课程人数同步更新；容量、重复、冲突、原因缺失提示明确 |
| 数据库一致性校验 | `backend/prisma/verify-c-load.ts` | enrolled_count 与 Enrollment 记录数一致 |
| 质量检查 | 后端 `typecheck`、C 组选课相关单测 | typecheck 通过；单测 605 个通过、1 个 live LLM 跳过 |

覆盖重点：培养方案确认、课程搜索、选课/退课事务、容量与冲突、先修成绩校验、FR-C-15 准入租约、课表查询、AI 推荐/解释/保存建议。

AI live 推荐样例：学生 `cstudent01` 请求推荐 2 门课程，偏好为计算机专业中期阶段、优先核心必修和高阶课程铺垫、避免明显冲突和先修风险、总学分控制在 18 左右。模型 `deepseek/deepseek-v4-flash-20260423` 正常返回，`preference` 阶段 27s / 1878 tokens，`recommendation` 阶段 21s / 1192 tokens，均为 HTTP 200、`finish=stop`。系统生成“稳妥稳选”“补短板优先”“轻负担优先”三套方案，代表课程包括 `CMAN-CS303 Database Systems`、`CMAN-CS304 Computer Networks`、`CMAN-CS311 Web Application Development`、`CMAN-STAT201 Probability and Statistics`、`CMAN-CS310 Low-Level Systems Programming`，推荐项均显示容量充足、无时间冲突、先修风险可控并通过硬性规则校验。

压测覆盖 200 在线用户、双后端实例准入租约、容量 50 抢课防超卖、容量 200 同时提交最终成功。容量 200 单次瞬时冲击会产生 429 受控并发提示，客户端短退避重试后全部成功。

补充人工验收：真实浏览器完整流程、教师名单导出、A/B/F 跨组链路和 Chrome/Edge 兼容性均已确认无问题。

遗漏项检查：C 组已覆盖自动化回归、压测、浏览器流程、AI live 样例、教务阶段管理、教务手动加课和 A/B/F 跨组链路；C 组暂无未完成验收项。总报告中 A/B/E/F 子系统仍有模板占位，需由对应组补齐统计、结论和风险。

---

## 7. 集成测试结果【全组统一写，相关组补充】

### 7.2 B-C 集成：排课结果与选课

C 组自动化验证：选课服务已读取 CourseOffering 与 Schedule，能够拒绝时间冲突课程，并在课程容量不足或状态关闭时拒绝选课。

C 组人工确认：A/B 提供的课程、开课和排课数据可支持 C 组课程查询、课表展示和冲突判断。

### 7.3 C-F 集成：选课结果与成绩管理

C 组自动化验证：先修课判断已接入成绩结果，按 SUBMITTED/CONFIRMED、及格线和有效成绩判断先修是否满足。成绩录入端到端流程仍需 F 组联调。

C 组人工确认：F 组成绩数据与 C 组先修课、学分进展链路联调无问题。

---

## 8. 专项测试【按实际实现选择填写】

### 8.1 权限与安全测试【A 组主写，全组补充】

| 测试点 | 预期结果 | 测试结果 |
|---|---|---|
| 学生选课接口不接收 student_id/studentId | 拒绝请求 | 通过，schema 与服务层均不信任前端学生身份 |
| AI 辅助选课不直接写入 Enrollment | 仅生成建议/解释 | 通过，AI 保存记录仅写建议快照 |

### 8.2 性能测试【C 组重点，全组补充】

| 场景 | 并发数 | 指标 | 结果 | 是否通过 |
|---|---:|---|---|---|
| 单实例选课核心流程 | 200 | login / enter / query / heartbeat / leave | 5 个阶段均 200/200 成功，总耗时 14.0s | 通过 |
| 双后端实例选课核心流程 | 200 | 本地 Docker 网络轮询两个后端实例 | 5 个阶段均 200/200 成功，总耗时 8.2s | 通过 |
| 可选课程查询 | 200 | 响应时间 | 单实例 p95 1504ms，双实例 p95 437ms，失败 0 | 通过 |
| 容量 50 抢课 | 200 | 无容量超卖 | 50 个成功、150 个容量满业务拒绝；DB enrolled_count=50、记录数=50 | 通过 |
| 容量 200 同时提交 | 200 | 最终成功与一致性 | 单次冲击 84 成功、116 个 429 受控提示；短退避重试后 200/200 成功，DB 记录数=200 | 通过 |
| 准入租约 | 200 + 单元模拟 | enter/heartbeat/leave/TTL | enter、heartbeat、leave 均 200/200 成功；重复进入、满员拒绝、超时释放单测通过 | 通过 |

### 8.3 兼容性测试

| 环境 | 测试内容 | 结果 |
|---|---|---|
| Chrome | C 组主要功能流程 | 通过 |
| Edge | C 组主要功能流程 | 通过 |

### 8.4 Web 界面与导航测试

C 组验证结果：学生选课、培养方案、课表和 AI 页面核心组件测试通过；真实浏览器导航、教师名单导出下载和多浏览器兼容性已人工确认通过。

C 组验证结果：教务阶段管理已完成 API 端到端验收和人工验收，覆盖 `/selection/admin/periods` 对应接口的阶段列表、ACADEMIC 权限、非教务写操作错误提示、全屏弹窗编辑、`allow_drop=false` 拒绝退课、`max_credits=1` 拒绝超学分选课、停用阶段拒绝选课，并已恢复测试阶段配置。教务手动加课已完成端到端人工验收，覆盖 `/selection/admin/manual-enrollment` 页面按学生学号/姓名和课程名称检索选择、填写加课原因后提交、学生选课记录与课表展示同步更新、课程人数同步增加，以及容量满、重复选课、时间冲突和原因缺失等失败提示。

### 8.5 数据一致性测试

C 组验证结果：选课创建/退课会同步维护 enrolled_count；课表查询读取选课结果与排课时间；先修课使用有效成绩结果判断。A/B/F 跨组真实数据链路已人工确认无问题。

---

## 9. 缺陷统计与分析【全组统一整合】

### 9.1 缺陷列表

| 缺陷编号 | 所属子系统 | 严重级别 | 标题 | 状态 | 负责人 | 关联用例 |
|---|---|---|---|---|---|---|
| BUG-C-001 | C | 中 | 准入租约并发写入时 Redis 状态可能被覆盖 | 已修复 | C 组 | TC-C-008, VC-C-13 |

### 9.2 按子系统统计

| 子系统 | 缺陷总数 | 已修复 | 未修复 | 严重缺陷 |
|---|---:|---:|---:|---:|
| C | 1 | 1 | 0 | 0 |

---

## 10. 回归测试结果【A-F 分组填写，统一汇总】

| 修复缺陷 | 回归用例 | 回归结果 | 是否引入新问题 |
|---|---|---|---|
| BUG-C-001 | TC-C-008, 200 在线用户压测 | 通过 | 否 |

---

## 11. 需求覆盖率与追踪矩阵【A-F 分组填写，统一整合】

| 需求编号 | 需求名称 | 测试用例 | 执行结果 | 是否覆盖 |
|---|---|---|---|---|
| FR-C-01~07 | 培养方案确认与学分进展 | TC-C-001 | 通过 | 是 |
| FR-C-08~13 | 课程搜索、详情和可选课程 | TC-C-002 | 通过 | 是 |
| FR-C-14~23 | 选课、退课和异常约束 | TC-C-003 ~ TC-C-006 | 通过 | 是 |
| FR-C-24~29 | 选课结果与学生课表 | TC-C-007 | 通过 | 是 |
| FR-C-35~36 | 选课连接控制与空闲释放 | TC-C-008 | 通过 | 是 |
| FR-C-38~43 | AI 辅助推荐、解释和保存建议 | TC-C-009 | 通过 | 是 |

---

## 12. 遗留问题与风险【全组统一写，A-F 补充】

| 编号 | 遗留问题 | 影响范围 | 风险等级 | 处理建议 |
|---|---|---|---|---|
| RISK-01 | 已完成本地单实例、双后端实例和并发提交压测；课程作业无公网部署条件，未覆盖真实公网链路 | C | 低 | 作为测试范围限制记录；若后续正式上线，再使用公网 URL 补充压测 |
| RISK-02 | AI 推荐已完成 1 个优质人工样例评估，但尚非生产级长期推荐质量评测 | C | 低 | 后续正式上线前扩大样本量并持续记录人工反馈 |

---

## 13. 测试结论【全组统一写】

### 13.2 各子系统结论

| 子系统 | 结论 | 说明 |
|---|---|---|
| C | 通过 | 自动化回归、200 在线用户、双后端实例、容量 50 防超卖、容量 200 并发提交、AI live 推荐样例、教务阶段管理和教务手动加课端到端验收均通过；浏览器流程、教师导出、A/B/F 链路和 Chrome/Edge 兼容性已通过 |

---

## 14. 附录

### 14.3 测试工具与脚本

C 组压测脚本：
- `backend/prisma/seed-c-load.ts`：生成 `cload001..cload200` 压测学生账号。
- `backend/prisma/verify-c-load.ts`：校验压测后 CourseOffering 计数和 Enrollment 记录一致。
- `scripts/course-selection-200-load-test.mjs`：模拟 200 个学生并发登录、准入、查询、心跳、释放、提交选课和 429 短退避重试。
