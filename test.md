# SMART-TEACHING-SERVICE-SYSTEM A 组前端联调测试清单

> 适用范围：A 组基础信息管理前端交互联调。  
> 对照文档：`docs/apis/A-information-management.md`。  
> 建议测试入口：前端 `http://localhost:5173`，后端 API `http://localhost:3000/api/v1`。  
> 默认账号可先使用 `admin / Admin123`、`teacher / teacher123`、`student / student123`，再补充新建用户场景。

## 0. 联调前置检查

- [ ] Docker 服务均正常：`server`、`web`、`postgres`、`redis` 可访问。
- [ ] 前端能打开登录页，刷新页面无白屏，控制台无启动级错误。
- [ ] 后端健康检查正常：`GET /api/health` 返回可用状态。
- [ ] 浏览器 Network 面板中 API 前缀为 `/api/v1`，无错误代理地址或跨域错误。
- [ ] 清空 localStorage 后访问任意受保护页面，会跳转到 `/login`。
- [ ] 登录后刷新页面，登录态能保持，菜单和用户信息仍正确。
- [ ] 所有接口错误提示不会只显示 `undefined`、`[object Object]` 或空白弹窗。

## 1. 通用页面交互规则

这些规则适用于用户、院系、专业、课程、培养方案、角色等所有列表页。

- [ ] 首次进入列表页会自动加载数据，并显示 loading 状态。
- [ ] 空数据时有可理解的空状态，不出现表格错位或白屏。
- [ ] 分页切换页码、每页条数后，列表和总数正确刷新。
- [ ] 搜索/筛选后分页回到合理页码，筛选条件不会丢失。
- [ ] 新增成功后关闭弹窗并刷新列表。
- [ ] 编辑成功后列表和详情数据同步更新。
- [ ] 删除前有确认弹窗，取消不会发请求，确认后刷新列表。
- [ ] 表单必填、格式、数字范围校验能阻止无效提交。
- [ ] 后端返回 400/409/422 时，页面能展示业务错误原因。
- [ ] 后端返回 401 时会清理登录态并跳转登录。
- [ ] 后端返回 403 时页面提示无权限，不误报为网络错误。
- [ ] 后端返回 404 时页面提示资源不存在，抽屉/弹窗状态可恢复。
- [ ] 连续快速点击提交按钮不会重复创建或重复提交。
- [ ] 弹窗关闭再打开时，旧表单数据和错误状态已清理。

## 2. 认证与个人账号

对照 API：`2.1` ~ `2.10`

| 测试项          | 前端操作                              | 对照 API                                   | 预期结果                                             | 执行结果                                                           |
| --------------- | ------------------------------------- | ------------------------------------------ | ---------------------------------------------------- | ------------------------------------------------------------------ |
| 登录成功        | 使用正确账号密码登录                  | `POST /api/v1/auth/login`                  | 跳转首页，保存 access/refresh token，菜单按角色显示  | 支持正常登录✔                                                      |
| 登录失败        | 输入错误密码                          | `POST /api/v1/auth/login`                  | 停留登录页，提示账号或密码错误                       | 已矫正                                                             |
| 登录表单校验    | 用户名/密码为空提交                   | `POST /api/v1/auth/login`                  | 前端阻止提交并显示必填提示                           | ✔                                                                  |
| 获取当前用户    | 登录后刷新页面或进入个人中心          | `GET /api/v1/auth/me`                      | 显示用户名、真实姓名、角色、权限等信息               | ✔                                                                  |
| Token 刷新      | 等待 access token 过期或模拟 401 刷新 | `POST /api/v1/auth/refresh`                | 能自动刷新并重试原请求；失败时跳转登录               | 暂时无法确认token是否刷新,但是就登陆失败跳转重定向而言是完全正确的 |
| 登出            | 点击退出登录                          | `POST /api/v1/auth/logout`                 | 清除本地登录态，跳转登录页，返回后不能访问受保护页面 | ✔                                                                  |
| 注册成功        | 打开注册页填写合法信息                | `POST /api/v1/auth/register`               | 注册成功并给出明确反馈                               | 缺少合格性提示.已修复并且提交分支                                  |
| 注册重复        | 使用已有用户名或邮箱注册              | `POST /api/v1/auth/register`               | 显示冲突提示                                         | √                                                                  |
| 忘记密码        | 输入有效邮箱发送重置邮件              | `POST /api/v1/auth/password/forgot`        | 显示发送成功或明确失败原因                           | 测试通过√                                                          |
| 重置 Token 校验 | 打开重置链接                          | `GET /api/v1/auth/password/reset/verify`   | 有效 token 显示重置表单，无效 token 显示失效提示     | 这个不确定暂时还没找到触发方法                                     |
| 重置密码        | 输入新密码并确认                      | `POST /api/v1/auth/password/reset/confirm` | 成功后可用新密码登录                                 | admin手动重置密码√                                                 |
| 修改密码        | 个人中心修改密码                      | `POST /api/v1/auth/change-password`        | 旧密码错误会提示；成功后旧密码不能登录               | √                                                                  |

## 3. 用户管理

对照 API：`3.1` ~ `3.20`

### 3.1 列表、统计、详情

- [ √] 进入“基础信息管理 / 用户管理”，能加载用户列表：`GET /api/v1/users`。
- [ √] 用户统计卡片或统计区域正确展示总数、学生、教师、管理员、启用/禁用数量：`GET /api/v1/users/stats`。
- [ √] 按关键词搜索用户名、真实姓名、邮箱、手机号，结果正确：`GET /api/v1/users?keyword=...`。
- [√ ] 按角色筛选学生、教师、管理员，结果正确：`GET /api/v1/users?role=...`。
- [√ ] 按状态筛选 ACTIVE/INACTIVE/BANNED，结果正确：`GET /api/v1/users?status=...`。
- [√ ] 打开用户详情，基础信息、角色、学生/教师/管理员扩展信息展示正确：`GET /api/v1/users/:id`。
- [ √] 打开不存在或已删除用户详情，页面能处理 404。

### 3.2 创建用户

- [√ ] 创建普通用户，填写用户名、密码、真实姓名、邮箱等：`POST /api/v1/users`。
- [ √] 创建学生用户，填写学号、专业、年级、班级，保存后详情显示学生信息。
- [ ] 创建教师用户，填写工号、院系、职称、办公地点，保存后详情显示教师信息。(略,感觉与学生类似,没测)
- [ ] 创建管理员用户，填写管理员类型、院系，保存后详情显示管理员信息。(略,同理)
- [ √] 用户名、邮箱、手机号、学号、工号重复时，页面显示冲突提示。
- [√ ] 密码强度、邮箱格式、手机号格式、必填项校验正确。
- [仅A-part而言是合理的 ] 角色选择为空或多角色场景符合业务预期。

### 3.3 编辑、删除、状态、密码、角色

- [√ ] 编辑用户基础信息：`PUT /api/v1/users/:id`。
- [√ ] 删除用户：`DELETE /api/v1/users/:id`，删除后列表不再显示或按筛选显示。
- [√ ] 禁用/启用/封禁单个用户：`PATCH /api/v1/users/:id/status`。
- [√ ] 批量修改用户状态：`PATCH /api/v1/users/batch/status`。
- [√ ] 管理员重置用户密码：`POST /api/v1/users/:id/password/reset`。
- [ √] 用户本人修改密码入口正常：`PATCH /api/v1/users/:id/password`。
- [ √] 分配用户角色：`POST /api/v1/users/:id/roles`。
- [√ ] 撤销用户角色：`DELETE /api/v1/users/:id/roles/:role_id`。
- [ √] 获取用户权限抽屉展示正确：`GET /api/v1/users/:id/permissions`。
- [ ] 当前登录用户被禁用或角色被撤销后，刷新页面权限表现正确。

### 3.4 批量、日志、头像、组织关系

- [ ] 批量创建用户成功和部分失败都能展示明细：`POST /api/v1/users/batch`。
- [ ] 批量导入时，重复用户、非法邮箱、缺少必填项会逐条标记失败。
- [ ] 系统日志页可分页、按用户/动作/时间筛选：`GET /api/v1/users/logs`。
- [ ] 上传合法头像后能立即预览并持久化：`POST /api/v1/users/:id/avatar`。
- [ ] 上传过大文件、非图片文件时有明确提示。
- [ ] 修改学生专业：`PATCH /api/v1/users/:id/student/major`。
- [ ] 修改教师院系：`PATCH /api/v1/users/:id/teacher/department`。
- [ ] 修改管理员院系：`PATCH /api/v1/users/:id/admin/department`。
- [ ] 对非对应身份用户执行学生/教师/管理员组织修改时，页面正确展示后端错误。

## 4. 院系管理

对照 API：`4.1` ~ `4.5`

- [ ] 院系列表分页加载：`GET /api/v1/departments`。
- [ ] 院系关键词搜索按名称/代码生效。
- [ ] 查看院系详情，显示代码、名称、负责人、联系方式、描述等：`GET /api/v1/departments/:id`。
- [ ] 创建院系：`POST /api/v1/departments`。
- [ ] 编辑院系：`PUT /api/v1/departments/:id`。
- [ ] 删除院系：`DELETE /api/v1/departments/:id`。
- [ ] 院系代码重复时显示冲突提示。
- [ ] 院系被专业、教师、管理员、课程引用时，删除失败提示应可理解。
- [ ] 删除成功后，相关下拉框和列表数据能刷新。

## 5. 专业管理

对照 API：`5.1` ~ `5.5`

- [ ] 专业列表分页加载：`GET /api/v1/majors`。
- [ ] 按关键词搜索专业名称/代码。
- [ ] 按院系筛选专业：`GET /api/v1/majors?department_id=...`。
- [ ] 查看专业详情，显示院系、学位类型、总学分等：`GET /api/v1/majors/:id`。
- [ ] 创建专业：`POST /api/v1/majors`。
- [ ] 编辑专业名称、总学分等：`PUT /api/v1/majors/:id`。
- [ ] 删除专业：`DELETE /api/v1/majors/:id`。
- [ ] 专业代码重复、院系不存在、总学分非法时显示错误。
- [ ] 专业被学生或培养方案引用时，删除失败提示应明确。

## 6. 课程管理

对照 API：`6.1` ~ `6.6`

- [ ] 课程列表分页加载：`GET /api/v1/courses`。
- [ ] 按课程名称/代码关键词搜索。
- [ ] 按院系、课程类型、状态筛选：`GET /api/v1/courses?department_id=...&course_type=...&status=...`。
- [ ] 查看课程详情，显示学分、学时、课程类型、开课院系、负责人、考核方式、先修课程：`GET /api/v1/courses/:id`。
- [ ] 创建课程：`POST /api/v1/courses`。
- [ ] 编辑课程：`PUT /api/v1/courses/:id`。
- [ ] 删除课程：`DELETE /api/v1/courses/:id`。
- [ ] 批量创建课程，成功/失败明细正确：`POST /api/v1/courses/batch`。
- [ ] 课程代码重复、学分/学时非法、院系不存在、教师不存在时显示错误。
- [ ] 先修课程选择、取消、保存后详情一致。
- [ ] 课程被培养方案、排课、选课等模块引用时，删除失败提示应明确。
- [ ] 新建或修改课程后，B/C/F 组相关课程下拉或列表能看到最新数据。

## 7. 培养方案管理

对照 API：`7.1` ~ `7.9`

- [ ] 培养方案列表分页加载：`GET /api/v1/curriculums`。
- [ ] 按专业筛选：`GET /api/v1/curriculums?major_id=...`。
- [ ] 按年份筛选：`GET /api/v1/curriculums?year=...`。
- [ ] 查看培养方案详情，显示总学分、必修学分、选修学分、课程清单：`GET /api/v1/curriculums/:id`。
- [ ] 创建培养方案：`POST /api/v1/curriculums`。
- [ ] 编辑培养方案：`PUT /api/v1/curriculums/:id`。
- [ ] 删除培养方案：`DELETE /api/v1/curriculums/:id`。
- [ ] 添加单门课程到培养方案：`POST /api/v1/curriculums/:id/courses`。
- [ ] 批量添加课程到培养方案：`POST /api/v1/curriculums/:id/courses/batch`。
- [ ] 从培养方案移除课程：`DELETE /api/v1/curriculums/:id/courses/:course_id`。
- [ ] 更新培养方案内课程类型、建议学期：`PUT /api/v1/curriculums/:id/courses/:course_id`。
- [ ] 总学分、必修学分、选修学分输入非法时前后端都能拦截。
- [ ] 同一课程重复加入培养方案时显示冲突提示。
- [ ] 删除或更新课程后，详情课程表同步刷新。
- [ ] C 组智能选课中学生培养方案、学分进度能读取到 A 组最新培养方案数据。

## 8. 角色、权限与令牌管理

对照 API：`8.1` ~ `8.11`

- [ ] 角色列表加载：`GET /api/v1/roles`。
- [ ] 按关键词筛选角色。
- [ ] 按 builtin 筛选内置/自定义角色。
- [ ] 查看角色详情，显示权限列表和基础信息：`GET /api/v1/roles/:id`。
- [ ] 创建自定义角色：`POST /api/v1/roles`。
- [ ] 编辑角色名称、描述：`PUT /api/v1/roles/:id`。
- [ ] 删除自定义角色：`DELETE /api/v1/roles/:id`。
- [ ] 内置角色不可删除或限制修改时，页面提示符合预期。
- [ ] 权限列表可按 resource/action/keyword 筛选：`GET /api/v1/permissions`。
- [ ] 为角色分配权限：`POST /api/v1/roles/:id/permissions`。
- [ ] 撤销角色权限：`DELETE /api/v1/roles/:id/permissions/:permission_id`。
- [ ] 权限保存后重新打开详情，勾选状态仍正确。
- [ ] 用户活跃令牌列表展示设备/IP/创建时间/过期时间：`GET /api/v1/users/:id/tokens`。
- [ ] 吊销指定令牌：`DELETE /api/v1/users/:id/tokens/:token_id`。
- [ ] 吊销用户全部令牌：`POST /api/v1/users/:id/tokens/revoke-all`。
- [ ] 吊销当前用户令牌后，当前会话退出或刷新后重新登录。

## 9. 权限与角色边界测试

对照 API：`9.1`、`9.2`

- [ ] `super_admin` 可看到并操作用户、角色、院系、专业、课程、培养方案等管理入口。
- [ ] `admin` 可见菜单和可操作按钮符合权限设计。
- [ ] `teacher` 登录后不能访问用户管理、角色权限等管理接口；直接输入 URL 也应被拦截或收到 403。
- [ ] `student` 登录后不能访问管理入口；直接输入 URL 也应被拦截或收到 403。
- [ ] 无权限按钮不应仅靠隐藏；直接调用接口时后端仍返回 403，前端能正确处理。
- [ ] 权限变更后，重新登录或刷新页面菜单会同步更新。
- [ ] 被禁用用户不能继续登录或继续调用受保护接口。

## 10. 跨组联调重点

A 组数据是其他模块的基础数据，以下场景建议和 B/C/F 组一起回归。

- [ ] 新建院系后，专业、课程、教师/管理员归属下拉能加载。
- [ ] 新建专业后，学生专业、培养方案、C 组学生培养方案查询能加载。
- [ ] 新建课程后，培养方案、B 组排课、C 组选课课程搜索、F 组成绩相关课程能加载。
- [ ] 修改课程学分后，培养方案详情、C 组学分进度、F 组成绩/GPA相关页面表现一致。
- [ ] 删除被引用的院系、专业、课程、培养方案时，不会造成其他组页面白屏。
- [ ] 修改用户角色后，C/D/E/F 组的菜单和受保护页面访问权限符合预期。
- [ ] 禁用教师或学生后，其他组依赖该身份的数据页面能给出明确提示。

## 11. 建议执行顺序

1. 登录、登出、刷新、个人信息。
2. 院系、专业基础数据。
3. 用户管理，分别创建学生、教师、管理员。
4. 角色权限和用户角色分配。
5. 课程管理。
6. 培养方案管理。
7. 批量导入、状态变更、日志、令牌吊销等高风险操作。
8. 使用 student/teacher/admin/super_admin 多角色账号做权限回归。
9. 与 B/C/F 组页面交叉验证 A 组基础数据是否同步。

## 12. Bug 记录模板

| 编号     | 页面     | 操作步骤      | 对照 API            | 实际结果 | 预期结果 | 账号/角色 | 严重级别 | 备注               |
| -------- | -------- | ------------- | ------------------- | -------- | -------- | --------- | -------- | ------------------ |
| A-FE-001 | 用户管理 | 1. ... 2. ... | `GET /api/v1/users` | ...      | ...      | admin     | 高/中/低 | Network 截图或响应 |
